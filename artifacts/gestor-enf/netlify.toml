[build]
  command = "pnpm --filter @workspace/gestor-enf run build"
  publish = "dist/public"
  environment = { PORT = "3000", BASE_PATH = "/" }

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
